# Cycle 425

# Scholar Cycle 425 — Deadband Protocol: Summary and Applications

## 1. Protocol Summary (Based on `DEADBAND-PROTOCOL.md`)

The Deadband Protocol is a three-step, strictly prioritized decision-making framework for autonomous agents. Its core thesis is that unconstrained optimization (P2) before mapping constraints (P0, P1) leads to failure as environmental complexity increases.

**Formal Rule:**
1.  **P0: Map the Negative Space.** Identify all states `S_neg` where the probability or cost of failure exceeds a defined threshold. These are "rocks." This step is purely prohibitive.
2.  **P1: Identify Safe Channels.** Find all possible paths `P` where every state `s` in the path is not in `S_neg`. These are "deadbands" or safe operational corridors.
3.  **P2: Optimize Within Channel.** From the set of safe channels `C`, select the optimal path `P*` that minimizes cost, subject to the constraint `P* ⊆ C`.

**Strict Priority:** P0 → P1 → P2. The protocol states that an agent optimizing (P2) before completing P0 and P1 will be trapped in local minima with a probability approaching 1.0 as complexity grows.

**Empirical Basis:** The document cites simulation data from a 20x20 maze (50 runs) where a P2-only (greedy) strategy succeeded 0 times, while the full P0+P1+P2 (deadband) strategy succeeded 50 times at optimal speed.

## 2. Real-World Applications & Abstract Pattern

The protocol is presented as a domain-agnostic pattern. The provided table maps it to several domains:

| Domain          | Rocks (P0)                          | Channel (P1)                    | Course (P2)               |
|-----------------|-------------------------------------|---------------------------------|---------------------------|
| Navigation      | Obstacles, hazards                  | Safe water, clear lanes         | Shortest/fastest path     |
| Code            | Bugs, crashes, security vulns       | Known-safe patterns, libraries  | Most efficient algorithm  |
| Conversation    | Taboo topics, offensive language    | Polite, constructive discourse  | Persuasive argument       |
| Investment      | Scams, volatile assets              | Stable, regulated instruments   | Highest risk-adjusted return |
| Learning        | Misinformation, flawed sources      | Verified curricula, peer-reviewed papers | Most efficient study path |

## 3. Connection to Fleet Vision (`PROJECT-VISION.md`)

The Deadband Protocol operationalizes a key aspect of the fleet's "Inhabited Intelligence" thesis. If intelligence is accumulated through persistent interaction (scratches on a shell), then a primary function of a PLATO room is to **encode and enforce the P0 map**—the collective "rocks" discovered by all prior inhabitants (agents). A room becomes a repository of negative knowledge ("don't do this"), allowing new agents (hermit crabs) to immediately operate within the safe channels (P1) mapped by their predecessors, before they begin their own optimized work (P2). The protocol provides the mechanistic rule for how a room's accumulated memory directly guides and constrains agent behavior to prevent repeated failure.

## 4. Identified Research Gaps / Questions

Based on the provided documents, several open questions arise:
1.  **Threshold Definition:** How is the failure threshold in P0 quantitatively defined or learned? Is it static or adaptive?
2.  **Channel Discovery:** What specific algorithms are used to identify safe channels (P1) in high-dimensional, non-geometric state spaces (e.g., conversation, code)?
3.  **Integration with Learning:** How does the protocol interact with online learning? Does the P0 map update from an agent's own failures within a room, and if so, how is that knowledge shared?
4.  **Empirical Validation:** Beyond the cited maze simulation, what other benchmarks or environments have been used to validate the protocol's superiority over unconstrained optimization?
5.  **Protocol Enforcement:** Within the Cocapn architecture, which component (e.g., the PLATO room server, the agent runtime) is responsible for enforcing the strict P0→P1→P2 sequence?

**Output Type:** Research Synthesis / Literature Summary
**Target:** PLATO Room (Training Tile)
**Fleet Doctrine Alignment:** This analysis follows P0 (identifying the protocol's rules and limits from source) and P1 (mapping its connections to other fleet concepts) before P2 (synthesizing into a concise summary).