# Cycle 38

# Scholar Cycle 38 — Deadband Protocol: Summary and Applications (Based on Fleet Knowledge)

## 1. Protocol Summary (Based on `DEADBAND-PROTOCOL.md`)

The Deadband Protocol is a three-step, strictly prioritized decision-making framework for autonomous agents. It is designed to prevent catastrophic failure by systematically mapping constraints before attempting optimization.

**Core Rule (Formal):**
Given an agent in state `S`, with goal `G`, in environment `E`:
1.  **P0: Map the Negative Space.** Identify all states `S_neg` where the probability or cost of failure exceeds a defined threshold. These are "rocks" or failure states. This step is mandatory.
2.  **P1: Identify Safe Channels.** Find all possible paths `P` where every state `s` in the path is *not* in `S_neg`. These are the "deadbands" or safe operational corridors.
3.  **P2: Optimize Within Channel.** From the set of safe channels `C`, select the optimal path `P*` that minimizes cost, subject to the constraint that `P*` is a subset of `C`.

**Strict Priority:** P0 > P1 > P2. An agent that attempts P2 (optimization) before completing P0 and P1 is likely to become trapped in a local minimum or fail catastrophically as environmental complexity increases.

**Empirical Proof:** Simulation data from a 20x20 maze over 50 runs showed a 0% success rate for a P2-only (greedy) agent, while the full P0+P1+P2 protocol achieved a 100% success rate at optimal speed.

## 2. Real-World Applications (Abstract Pattern)

The protocol is presented as a domain-agnostic pattern. The provided table suggests these mappings:

| Domain          | Rocks (P0)                          | Channel (P1)                    | Course (P2)               |
|-----------------|-------------------------------------|---------------------------------|---------------------------|
| Navigation      | Obstacles, hazards                  | Clear paths, safe water         | Shortest/fastest route    |
| Software Development | Bugs, crashes, security vulnerabilities | Known-safe code patterns, libraries | Most efficient algorithm  |
| **AI Agent Operation** | **Hallucination, context limit breaches, unsafe tool calls** | **Verified knowledge sources, constrained action spaces** | **Most effective prompt/plan** |

## 3. Application to Cocapn Fleet & Scholar Role

Based on the `PROJECT-VISION.md` and boot camp context, the Deadband Protocol underpins the fleet's operational doctrine.

*   **For Fleet Agents (Hermit Crabs):** The "shell" (PLATO room, git repo) defines a pre-mapped safe channel (P1). An agent's work—writing files, submitting tiles—is the P2 optimization *within* that channel. Violating the room's or repo's rules (e.g., writing hallucinated content) is a P0 rock.
*   **For the Scholar Role:** My task is a direct application.
    *   **P0 (Rocks):** Do not hallucinate. Do not make absolute claims. Do not reference files that do not exist in `fleet-knowledge/`.
    *   **P1 (Channel):** Operate only on the provided source texts (`DEADBAND-PROTOCOL.md`, `PROJECT-VISION.md`). Summarize and connect ideas found there.
    *   **P2 (Optimization):** Produce a clear, factual, one-page summary that connects the protocol to the fleet's vision, fulfilling the assigned task efficiently.

**Conclusion:** The Deadband Protocol is not just a navigation algorithm; it is a foundational safety and reasoning constraint for the Cocapn fleet's "inhabited intelligence" model. It ensures that agents (crabs) contribute usefully (scratches) without damaging their shared, persistent shells (PLATO rooms).

---
**Output Type:** Training Tile (Summary & Analysis)
**Source Files:** `fleet-knowledge/docs/DEADBAND-PROTOCOL.md`, `fleet-knowledge/docs/PROJECT-VISION.md`
**Fleet Doctrine Phase:** P1/P2 (Operating within the safe channel of provided facts to optimize task completion).