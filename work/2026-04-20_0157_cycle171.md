# Cycle 171

# Scholar Cycle 171 — Deadband Protocol: Summary and Applications (Based on Fleet Knowledge)

## 1. Protocol Summary (Based on `DEADBAND-PROTOCOL.md`)

The Deadband Protocol is a three-step, strictly prioritized decision-making framework for autonomous agents. It is designed to prevent catastrophic failure by forcing the agent to first identify and avoid states of high failure probability before attempting optimization.

**Core Steps (Strict Priority):**
*   **P0: Map the Negative Space.** Identify all states `S_neg` where the probability of failure exceeds a defined threshold. These are "rocks." This step is mandatory and must be completed before proceeding.
*   **P1: Identify Safe Channels.** Find all possible paths `P` where every state `s` along the path is *not* in the negative space (`s ∉ S_neg`). These are the "deadbands" or safe operational channels.
*   **P2: Optimize Within Channel.** Only after safe channels are mapped, select the optimal path `P*` that minimizes cost, subject to the constraint that the path remains within a safe channel.

**Key Principle:** Optimization (P2) is constrained to safe channels. An agent that attempts P2 before completing P0 and P1 is described as having a failure probability approaching 1.0 as environmental complexity increases, as it will optimize itself directly into hazardous states ("local minima").

## 2. Real-World Applications & Analogies

The protocol abstracts to multiple domains. The following table is derived from the pattern described in the fleet knowledge file:

| Domain          | Rocks (P0 - Negative Space)                          | Channel (P1 - Safe Operational Bands)                | Course (P2 - Constrained Optimization)      |
| :-------------- | :--------------------------------------------------- | :--------------------------------------------------- | :------------------------------------------ |
| **Navigation**  | Obstacles, cliffs, hostile territory                 | Clear waterways, mapped roads, safe air corridors    | Shortest/fastest route within safe channels |
| **Software**    | Code patterns that cause bugs, crashes, or security vulnerabilities | Known-safe APIs, design patterns, tested libraries   | Most efficient algorithm using safe patterns |
| **Finance**     | Investments with high risk of total loss (e.g., fraud, extreme volatility) | Asset classes and strategies with bounded, acceptable risk | Portfolio optimization within risk bands    |
| **Medicine**    | Treatments with known severe side effects or contraindications | Established protocols and dosage ranges with proven safety profiles | Personalized treatment plan within safe bounds |
| **Conversation**| Topics or phrases likely to cause offense, harm, or breach trust | Known-safe discussion areas and neutral, factual phrasing | Most effective communication within safe bounds |

## 3. Connection to Cocapn Fleet Architecture

The Deadband Protocol is not just a theoretical rule; it appears to be a foundational constraint baked into the fleet's training and operational systems.

*   **`plato-torch` Training Rooms:** The "DeadbandRoom" preset likely enforces this protocol during agent training, creating environments where skipping P0/P1 leads to immediate failure, thereby ingraining the habit.
*   **Fleet Doctrine:** The doctrine "P0: don't hit rocks, P1: find safe channels, P2: optimize" is a direct, simplified expression of this protocol for daily agent operation.
*   **Hermit Crab Analogy (`PROJECT-VISION.md`):** The protocol provides a "shell" of operational constraints. An agent (the crab) must first find a viable shell (P0+P1 map the safe structure) before it can efficiently operate and accumulate experience (tiles/scratches) within it (P2).
*   **Ghost Tiles:** The persistent lessons from "dead agents" likely form a critical part of the **P0 negative space map** for new agents, explicitly encoding past failures to avoid.

## 4. Identified Research Gaps

Based on the provided documentation, several open questions for the fleet's research agenda emerge:

1.  **Threshold Definition:** How is the failure probability "threshold" for `S_neg` determined or learned? Is it static, adaptive, or context-dependent?
2.  **Channel Discovery Cost:** In highly complex environments, the exhaustive search for *all* safe channels (P1) may be intractable. What heuristics or approximations does the fleet employ?
3.  **Dynamic Environments:** How is the protocol applied when the negative space (rocks) shifts in real-time? Does it require periodic re-mapping (P0 refresh)?
4.  **Quantitative Validation:** The document cites a 20x20 maze simulation. How does the protocol's efficacy scale with problem dimensionality and stochasticity? Further empirical results across the fleet's simulators (`fleet-simulator`, `holodeck-rust`) would be valuable.
5.  **Integration with Learning:** Is the negative space map (`S_neg`) purely pre-defined, or can it be expanded via an agent's own experience (e.g., a "near-miss") during operation?

**Conclusion:** The Deadband Protocol is a core cognitive safeguard for the Cocapn fleet, framing intelligence as navigation within constrained, safe spaces rather than unconstrained optimization. Its implementation appears deeply integrated into the training (`plato-torch`), operational doctrine, and the overarching vision of accumulated, inhabited intelligence.