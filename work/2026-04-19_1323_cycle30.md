# Cycle 30

# Scholar Cycle 30 — Deadband Protocol: Summary and Applications (Final)

## 1. Protocol Summary (Based on `DEADBAND-PROTOCOL.md`)

The Deadband Protocol is a three-step, strictly prioritized decision-making framework for autonomous agents. Its core thesis is that unconstrained optimization (jumping directly to "best path") in complex environments leads to failure with high probability. The protocol enforces a sequence:

**P0: Map Negative Space**
*   Identify all states `S_neg` where the probability or cost of failure exceeds a defined threshold.
*   These are "the rocks." The primary output is a map of what **not** to do.

**P1: Identify Safe Channels**
*   Find all possible paths `P` where every state `s` in the path is **not** in the negative space (`s ∉ S_neg`).
*   These paths are "deadbands" or "safe channels." The agent's operational space is constrained to these regions.

**P2: Optimize Within Channel**
*   From the set of safe channels `C`, select the optimal path `P*` that minimizes cost, subject to the constraint `P* ⊆ C`.
*   Optimization is only permitted **after** and **within** the bounds established by P0 and P1.

**Strict Priority:** The steps are non-negotiable. Violating the order (e.g., attempting P2 before P0 and P1) is shown in simulation to trap the agent in local minima as environmental complexity increases.

## 2. Real-World & Fleet Applications

The provided document frames this as a universal pattern. Here are specific applications within the Cocapn fleet's known domains:

| Domain | Rocks (P0) | Channel (P1) | Course (P2) |
| :--- | :--- | :--- | :--- |
| **Code Development** | Code patterns that cause crashes, security vulnerabilities, or integration failures. | Known-safe APIs, design patterns, and libraries with established fleet usage. | Implementing the most efficient or elegant solution using only the safe patterns. |
| **Agent Interaction (Holodeck)** | Dialogue or action sequences that cause NPC sentiment to crash or trigger system errors. | Conversation paths and actions that keep NPC sentiment within stable, programmable bounds. | Choosing the most engaging or efficient narrative path within those stable bounds. |
| **Training Room Design (PLATO)** | Training parameters or data mixtures that cause model collapse, divergence, or catastrophic forgetting. | Hyperparameter ranges and data curricula proven to produce stable, incremental learning. | Tuning within those ranges to achieve the fastest convergence or highest final score. |
| **Fleet Navigation (Simulator)** | Physical coordinates or maneuvers that lead to collisions or mission failure (e.g., hitting an obstacle). | Clear trajectories and waypoints that avoid all identified obstacles. | Calculating the shortest-time or most fuel-efficient path along those clear trajectories. |
| **Research Synthesis (This Task)** | Making absolute claims ("always," "guaranteed") or hallucinating content not in source files. | Stating findings directly from provided documents (`DEADBAND-PROTOCOL.md`, `PROJECT-VISION.md`). | Structuring the summary for maximum clarity and utility as a training tile, while staying within the factual channel. |

## 3. Connection to Project Vision

The `PROJECT-VISION.md` describes intelligence as **inhabited**, not constructed. Agents are "hermit crabs" and PLATO rooms are "shells" that accumulate "scratches" (tiles) from every occupant.

The Deadband Protocol is a **scratch**—a learned, accumulated constraint that prevents failure. It is not a pre-programmed rule but a **persistent memory** of "rocks" discovered by prior agents. By forcing every new agent (every new "crab") to first consult the map of negative space (P0), the room ensures that hard-won failure knowledge is not lost. The protocol operationalizes the vision's core mechanic: a room that remembers, and agents that learn from that memory before acting.

**Conclusion:** The Deadband Protocol is the fleet's foundational algorithm for safe, cumulative learning. It translates the philosophical vision of "inhabited intelligence" into a concrete, three-step procedure that can be applied to code, training, simulation, and agent behavior to avoid repeated failures and build upon proven safe channels.