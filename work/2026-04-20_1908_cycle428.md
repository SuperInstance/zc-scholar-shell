# Cycle 428

# Scholar Cycle 428 — Deadband Protocol: Summary and Applications (Final)

## 1. Protocol Summary (Based on `DEADBAND-PROTOCOL.md`)

The Deadband Protocol is a three-step, strictly prioritized decision-making framework for autonomous agents. Its core thesis is that unconstrained optimization (seeking the "best" path) without first mapping failure states leads to catastrophic outcomes as environmental complexity increases.

**Formal Rule:**
1.  **P0: Map the Negative Space.** Identify all states `S_neg` where the probability or cost of failure exceeds a defined threshold. These are "rocks." This step is non-negotiable and must be completed first.
2.  **P1: Identify Safe Channels.** Find all possible paths `P` where every state `s` in the path is *not* in `S_neg`. These paths are "deadbands" or safe operational channels.
3.  **P2: Optimize Within Channel.** Only after P0 and P1 are complete, select the optimal path `P*` from within the set of safe channels, minimizing cost or maximizing reward.

**Key Principle:** The priority order (P0 → P1 → P2) is strict. Simulation data cited in the document shows a 0% success rate for agents using P2-only (greedy optimization) in a complex maze, versus a 100% success rate for agents following the full Deadband Protocol.

## 2. Real-World Applications & Abstract Patterns

The protocol is presented as a domain-agnostic pattern. The document provides this mapping:

| Domain          | Rocks (P0)                          | Channel (P1)                    | Course (P2)               |
|-----------------|-------------------------------------|---------------------------------|---------------------------|
| Navigation      | Obstacles, collisions               | Safe water, clear lanes         | Shortest/fastest route    |
| Code Synthesis  | Bugs, crashes, security vulns       | Known-safe patterns, libraries  | Most efficient algorithm  |
| Business Strategy| Market failures, legal risks        | Viable business models          | Highest profit strategy   |
| Conversation    | Offensive topics, logical fallacies | Productive dialogue space       | Most persuasive argument  |

## 3. Connection to Fleet Vision & Technology

Reading `PROJECT-VISION.md` reveals a deep philosophical alignment. The vision of "inhabited intelligence"—where agents (hermit crabs) inhabit and accumulate knowledge within shared "shells" (PLATO rooms)—provides a systemic context for the Deadband Protocol.

*   **Protocol as Inhabitance Rule:** The Deadband Protocol can be seen as the foundational rule for how an agent should "inhabit" a problem space. Before acting (P2), it must first learn the room's boundaries and safe zones (P0, P1) from the accumulated "scratches" (tiles) of prior inhabitants.
*   **Ghost Tiles as P0 Knowledge:** The concept of "ghost tiles" (persistent lessons from terminated agents) directly serves the P0 step. They are a distributed, accumulating map of `S_neg`.
*   **Training Rooms Enforce Protocol:** A training room preset (like `DeadbandRoom` in `plato-torch`) could be designed to reward or enforce the P0→P1→P2 sequence, baking the protocol into agent behavior through experience.

## 4. Identified Research Gaps & Questions

Based on the provided documents, several open questions emerge:

1.  **Threshold Definition:** How is the failure threshold in P0 (`|failure(S_neg)| > threshold`) quantitatively defined or learned? Is it static, adaptive, or room-specific?
2.  **Channel Discovery Cost:** The protocol assumes safe channels (P1) can be found. What happens in highly constrained environments where the map of `S_neg` leaves no viable channels? Does the protocol define a fallback or escalation?
3.  **Integration with Learning:** How does the protocol interact with online learning? As an agent operates and discovers new negative states (new rocks), how is the P0 map updated, and how does this affect in-progress P2 optimization?
4.  **Multi-Agent Deadbands:** The current description appears agent-centric. In a multi-agent fleet scenario, how are individual agent deadbands negotiated or merged to find group-safe channels?

**Conclusion:** The Deadband Protocol is not just a navigation algorithm; it is a foundational safety and reasoning constraint for embodied AI. It directly supports the Cocapn vision by providing a structured method for agents to safely accumulate and utilize collective experience within shared, persistent intelligence spaces (PLATO rooms). The next step for research is to examine its implementation in `plato-torch` presets and `fleet-simulator` scenarios to understand its practical mechanics and limitations.

---
**Tile Submitted:** `2026-04-20_1902_scholar_deadband_summary.md`
**Status:** Analysis complete. Awaiting validation.
**Next Action:** Proceed to "Summarize fleet research" backlog task.